# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# LP Content Engine, Claude Context

## For Claude: keep this file current

After any change that affects how the system works, new files, changed conventions, new scripts, toggled features, renamed artists, architectural decisions, update this file before ending the session. Future Claude has no memory of this conversation; this file is the handoff. If something was decided but not yet activated (e.g. a commented-out feature), note it here so it doesn't get re-implemented or re-debated.

## What this is

Automated weekly social media pipeline for **Love Productions** (loveproductions.com), a New York booking agency (est. 1985, 180+ acts, 70+ countries). The engine turns news about tribute acts and their original artists into platform-native LinkedIn/Instagram/Facebook draft posts, queued in Buffer for human review before publishing.

## Pipeline (end to end)

```
Airtable calendar (appXLETHThc0p5MOz) → upcoming FE shows (next 7 days)
  → Tour dates sheet (TOUR_DATES_SHEET_ID) → ticket URL lookup
  → Google Sheets dedup → Claude Sonnet show announcement posts → Buffer drafts

Airtable artists (appMMwX47V1g2Sv5u, priority-ordered)
  → Claude Haiku web search (news per artist) + whitelisted music-news RSS feeds (lp/feeds.py)
  → Google Sheets dedup → Claude Sonnet content generation → Buffer drafts
  [Phase 4] → remaining slots → Airtable contracts re-bookings (act booked twice by one venue) → Buffer drafts
  [Phase 5] → remaining slots → Claude Haiku testimonial search (source-verified) → Buffer drafts
  [Phase 6] → remaining slots → act spotlights scraped from each act's LP page → Buffer drafts
  [Phase 7] → remaining slots → agency credibility post (LinkedIn only) → Buffer draft
  [Phase 8] → remaining slots → Claude Haiku trivia search (original artists) → Claude Sonnet posts → Buffer drafts
  [Phase 9] → still-remaining slots → Claude Haiku historical fact search → Claude Sonnet posts → Buffer drafts

Buffer analytics → top performers → injected as style examples into every generate_posts() call

Every topic queued to Buffer (shows, artist news, trivia, historical facts)
  → Claude Sonnet long-form article (generate_article) → LP News WordPress plugin → draft post on loveproductions.com
```

Show announcements and artist news compete for slots first (Phases 1-3), then the buyer-proof fills (re-bookings, testimonials, act spotlights, agency credibility, Phases 4-7), then the Instagram/Facebook filler (trivia and historical facts, Phases 8-9). All phases feed the same dedup store and Buffer queue. **Every** topic that gets queued to Buffer also gets a dedicated long-form article drafted as a standard WordPress post on loveproductions.com (see "Website news posts" below).

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env  # then fill in keys
```

## Commands

```bash
python main.py                              # full weekly run
python main.py --dry-run                    # preview only, no Sheets or Buffer writes, full post text printed
python main.py --artist "Concert of Kings"  # single artist
python main.py --test-calendar              # print upcoming shows from Airtable calendar and exit
python main.py --test-rebookings            # print act/venue pairings with 2+ FE contracts and exit
python main.py --test-analytics             # print top-performing Buffer posts with engagement scores and exit
python main.py --tour-carousel [--dry-run]  # draft the "tours on sale now" carousel (one tour-date slide per act)
python main.py --test-clips [--artist NAME] # cut a short Vimeo clip per act for review, then exit
python main.py --visual-count N [--dry-run]  # visual-only mode: N graphic/video posts, queued, no Claude calls

python main.py --news-count N               # news-only mode: create up to N news posts queued to Buffer (no schedule), skip shows

python main.py --news-from-buffer           # convert current Buffer FB drafts (status 'draft') into website news posts and exit
python main.py --news-from-queue            # convert current Buffer FB queue (status 'scheduled') into website news posts and exit
python main.py --news-from-week [DAYS]      # convert past DAYS (default 7) of sent FB posts into website news posts and exit

python clean_up.py          # preview expired show draft purge
python clean_up.py --apply  # delete expired show drafts
```

## Key files

| File                                         | Purpose                                                                                                                                                                                                              |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `main.py`                                    | Thin orchestration layer + CLI entry point                                                                                                                                                                           |
| `clean_up.py`                                | Buffer draft maintenance, fixes em/en dash spacing and purges expired show announcement drafts                                                                                                                      |
| `lp/config.py`                               | All env vars, constants, cost tracking state + helpers, `load_env()`                                                                                                                                                 |
| `lp/loaders.py`                              | `load_skill_graph()`, `load_artist_mappings()`                                                                                                                                                                       |
| `lp/airtable.py`                             | `fetch_airtable_artists()`, `fetch_upcoming_shows()`, `show_to_topic()`, `fetch_venue_from_contracts()`                                                                                                              |
| `lp/sheets.py`                               | Google Sheets read/write, `read_used_topics()`, `mark_topics_used()`, `mark_show_used()`, `lookup_ticket_url()`, `lookup_venue_name()` (venue names for re-bookings)                                                |
| `lp/ai.py`                                   | Claude helpers, `search_artist_news()`, `search_trivia()`, `search_historical_facts()`, `score_and_rank_topics()`, `generate_posts()`, `generate_article()`, `default_categories()`, `format_performance_context()` |
| `lp/buffer.py`                               | Buffer GraphQL, `discover_buffer_profiles()`, `post_draft_to_buffer()`, `fetch_top_performers()`, `purge_expired_show_drafts()`, `test_buffer()`                                                                    |
| `lp/wordpress.py`                            | LP News client, `publish_news_posts()` posts a batch of news items to the LP News WordPress plugin (draft posts on loveproductions.com)                                                                             |
| `lp/scrape.py`                               | `fetch_og_image()`, cheap og:image scrape of a topic's source URL (no AI call), for native image assets; `fetch_page_prose()` (act-page copy for spotlights), `verify_quote_on_page()` (testimonial anti-fabrication guard) |
| `lp/feeds.py`                                | `load_feed_items()`, `search_artist_feeds()`, whitelisted music-news RSS ingestion (no AI call), a second news channel alongside web search                                                                         |
| `lp/cards.py`                                | `render_quote_card()`, `render_stat_card()`, `render_tour_card()`, `render_tour_poster()`, Pillow-rendered branded image cards                                                                                       |
| `make_tour_posters.py`                       | Standalone CLI: renders a tour poster per act from key art + the tour dates sheet. See "Generated tour posters"                                                                                                     |
| `lp/stats.py`                                | `agency_stats()`, `rebooking_stat()`, `act_credential_stats()`, verified hard numbers for stat cards                                                                                                                |
| `lp/artist_links.py`                         | `ARTIST_PAGE_URLS` static roster map + `lookup_artist_url()`, prepopulated `/title-item/` page links so `artist_url` never blanks at runtime                                                                        |
| `content-skill-graph/`                       | Knowledge base loaded as Claude's system prompt at generation time                                                                                                                                                   |
| `content-skill-graph/index.md`               | Master index + execution instructions for content generation                                                                                                                                                         |
| `content-skill-graph/engine/artists.md`      | Tribute act → original artist mappings (markdown table)                                                                                                                                                              |
| `content-skill-graph/engine/scoring.md`      | Scoring criteria for ranking candidate topics before generation                                                                                                                                                      |
| `content-skill-graph/engine/agency-facts.md` | The **only** facts allowed in an agency credibility post. Client-verified; nothing may be added without sign-off                                                                                                     |
| `content-skill-graph/voice/reference-posts.md` | **Read first.** The rejected post + diagnosis, and the Reliant post the client wants imitated                                                                                                                     |
| `content-skill-graph/voice/brand-voice.md`   | Core personality, vocabulary, banned words                                                                                                                                                                           |
| `content-skill-graph/voice/platform-tone.md` | Per-platform tone adaptations                                                                                                                                                                                        |
| `content-skill-graph/voice/humanizer.md`     | AI writing patterns to strip from every post                                                                                                                                                                         |

## Important decisions and conventions

**Skill graph as system prompt.** All 13 markdown files in `content-skill-graph/` are concatenated and passed as the system prompt to every `generate_posts()` call. Editing any `.md` file there immediately affects generation, no code change needed.

**Skill-graph rewrite (2026-07-31).** After the client rejected a post as "so AI" (see "IN PROGRESS" below), the graph was reworked rather than patched. What changed:
- **New file `voice/reference-posts.md`, and it is the most important file in the graph.** It holds the rejected post verbatim with a six-point diagnosis of why it failed, a rewritten version, and the Reliant Talent Agency post the client asked us to imitate (17 words over a 7-slide carousel). Rules describe; worked examples calibrate. `index.md`, `humanizer.md` and `linkedin.md` all point at it.
- **LinkedIn length dropped from 500-700 characters to 200-300 characters of copy** (the booking link does not count toward the budget), hard ceiling 400, two or three sentences. Changed in `linkedin.md`, `platform-tone.md`, `repurpose.md` and the `generate_posts()` prompt together, so nothing contradicts.
- **The LinkedIn CTA is now Steve's calendar link, not the info@ email** (client direction). `generate_posts()` injects `config.STEVE_CALENDAR_LINK` via `booking_cta_instruction`, since the model cannot use a URL it was never given; the graph only describes the rule. Falls back to the email when the env var is unset.
- **`index.md` rewritten.** It was badly stale (still advertised LinkedIn at 1,300-2,000 characters and claimed shows bypass scoring). It now opens with three override rules: short, one hard fact up front, and the graphic carries the message.
- **`humanizer.md` gained a "Length is the loudest tell" section** placed above every other pattern, plus entries for "the kind of X that Y" and claims dressed as evidence, with the rejected post's two symmetrical constructions quoted as the worked example.
- **`buyers.md` gained a "How NOT to talk to them" section** (they work in live entertainment; do not explain the business to them) and its content list now matches what the engine actually produces, with post-show recaps explicitly marked as deliberately not produced.
- **Verified by real generation, not by inspection.** Two topics run end to end: an act spotlight came back at 296 characters and a re-booking at 301 (156 of copy), both leading with the hard fact, both zero em dashes, both using the calendar CTA.

**Earlier length work (client direction 2026-07-24), superseded above.** The client found output too long and too AI-sounding. Two levers: (1) length (the figures in this paragraph are historical, see the rewrite above for current targets), and `generate_article()` writes 2 to 3 short paragraphs. `generate_posts()` and `generate_article()` both carry explicit length + "sound human" instructions in `lp/ai.py` on top of the skill-graph rules. (2) anti-AI, `humanizer.md` was expanded: more banned AI words, plus rules against the rule-of-three, symmetrical "not just X but Y" sentences, teaser scaffolding, rhetorical-question openers, uniform sentence length, and adjective-over-specifics writing. **Real brand voice** now lives in `brand-voice.md` under "Real Brand Voice (from loveproductions.com)", verbatim copy pulled from their About page, act pages, and a news article (credentials-first, specific venues/awards/years, warm). There's an HTML-comment placeholder there to paste more client-approved examples; the more real examples, the closer generation stays to their voice.

**Two models.** Haiku (`SEARCH_MODEL`) for news search and anything cheap/mechanical. Sonnet (`CONTENT_MODEL`) for content generation. Never swap them without considering cost impact.

**API refusals abort the run's Claude work (2026-07-31).** Every search helper catches its exception and returns `[]`, which is indistinguishable from "nothing found". When a real run hit the account usage limit, it walked the entire roster logging "No trivia found" for each act and reported success with **0 topics**, which would have been a silently empty week. `config.record_api_exception()` now flags account-level refusals (matched on "usage limit", "credit balance", "quota", "billing", since Anthropic returns these as **400 invalid_request_error**, not 429) and sets `config.api_blocked`. `under_cost_cap()` returns False while that is set, and since every Claude call is already gated on it, one check short-circuits the whole run. Transient failures (timeouts, bad JSON) deliberately do **not** set it.

**Cost cap.** Default $5/run (`COST_CAP_USD`). Each full topic costs ~$0.06 (1 search + 1 generate). Cap is checked before every Claude call via `config.under_cost_cap()`. Mutable cost state lives in `lp/config.py` (`estimated_cost_usd`, `claude_call_count`) and is mutated directly from `lp/ai.py`.

**Buffer rate limits.** Two distinct limits:
- *Per-minute:* 60 requests/minute. Both `main.py` and `clean_up.py` read `retryAfter` from 429 responses and retry. If no `retryAfter`, defaults to 61s wait.
- *Daily:* Hard 24h cap per API client (`"window":"24h"` in the 429 body). When hit, `clean_up.py` exits immediately with a clear message. Nothing to do but wait until the next day. Running `main.py` followed immediately by `clean_up.py` is enough to exhaust the daily limit, so space them out.
- Both scripts use the GraphQL API at `https://api.buffer.com`. The legacy REST API (`api.bufferapp.com`) does **not** accept OIDC tokens, don't use it.

**Buffer GraphQL notes.** Fetching posts requires `organizationId` (not `channelId`) in `PostsInput`. Updating posts uses `editPost` mutation (not `updatePost`). **Deleting posts:** `deletePost` returns the union `DeletePostPayload` whose members are `DeletePostSuccess { id }` and `VoidMutationError { message }`, **not** the `PostActionSuccess`/`MutationError` union used by create/edit. Spreading the wrong fragments causes a silent GraphQL 400 (`_buffer_gql` then returns `{}`); the delete helpers now treat success as `"id" in result` so an empty/errored response is logged as a failure, not a false "Deleted". Both delete paths (`purge_expired_show_drafts()` in `lp/buffer.py` and `_delete_post()` in `clean_up.py`) use this corrected shape. Facebook edits require `metadata.facebook.type`. Instagram edits require `metadata.instagram.type` + at least one image (use the LP logo placeholder). **Note:** Buffer removed `status` and `after` (pagination cursor) from `PostsInput` in a 2026 API update. `get_occupied_slots()` now fetches a single page only (no pagination), sufficient for checking this week's 7 slots. `fetch_top_performers()` uses a `statistics` field that may or may not be available; it degrades gracefully to `[]` if not.

**No em dashes (client rule).** Em dashes are banned everywhere: social posts, website articles, and the docs/code in this repo. The skill graph enforces **zero em dashes per post** (see `humanizer.md`), and `generate_article()` repeats the rule. Use a comma, a colon, a semicolon (sparingly), or a full stop and a new sentence instead. En dashes ("–") are out too. The `replace(" — ", "—")` normalization still runs in `main.py` as a spacing safety net for any stray dash that slips through, but the goal is that none appear in the first place.

**Show announcements (calendar pipeline).** `fetch_upcoming_shows()` pulls from Airtable base `appXLETHThc0p5MOz` (env: `AIRTABLE_CALENDAR_BASE_ID`), table `tblK2LMog1WUEv3j0`. Filters for `LPC Contract Status = "(FE) Fully Executed"` and show dates within the next 7 days (`SHOW_DAYS_AHEAD`). Dedup key is `lpc_{LPC #}` stored in the url column of Sheets. Test with `python main.py --test-calendar`. **As of the interleave change (2026-07-13), show announcements no longer bypass scoring or claim slots first**, see "Scoring and scheduling" below. Each upcoming show is collected as a candidate (ticket/venue enrichment happens at collection time, flagged `_is_show=True`, carries `_show` for `mark_show_used()`), given a baseline `_score = config.SHOW_BASE_SCORE + exclusivity_bonus(...)`, and competes head-to-head with news in one ranked pool. Consequence: when shows + news exceed the 7 weekly slots, the lowest-scoring items, **shows included**, are dropped and not announced.

**Artist names are exact.** The `artists.md` table maps tribute act names exactly as they appear in Airtable. Claude searches for the exact name, do not paraphrase. "Concert of Kings" (not "Elvis: The Concert of Kings"). Priscilla Presley is a separate act from Concert of Kings. **Note:** Airtable still has the old name "Elvis: The Concert of Kings", needs to be updated there too for the mapping to work correctly.

**Expired draft cleanup.** `purge_expired_show_drafts()` lives in `lp/buffer.py` but is **commented out** at the top of `main()` in `main.py`. It detects show announcement drafts with past dates and deletes them. Uncomment when ready, always test with `--dry-run` first. The same logic is active (not commented out) in `clean_up.py --purge-expired`.

**Dry run output.** `main.py --dry-run` prints full post text (not truncated) with scheduled slot per post. Safe to run to preview output without writing to Sheets or Buffer.

**Scoring and scheduling (shows + news interleaved by score).** As of 2026-07-13, shows and news compete for the same 7 weekly slots in **one ranked pool**, shows no longer claim the earliest slots up-front (this fixed the bug where a full week of Airtable shows left artist news with zero slots). `main.py` runs these phases:
1. **Collect show candidates**, each upcoming Airtable show → topic, ticket/venue enrichment, `_is_show=True`, `_score = config.SHOW_BASE_SCORE + exclusivity_bonus(priority, act)`. No generation/posting yet. (Skipped in news-only mode.)
2. **Collect news candidates**, search all artists (web search + RSS feeds), dedup vs. `used`.
3. **Score news** in one Haiku call (`score_and_rank_topics()` in `lp/ai.py`): five criteria (relevance, freshness, velocity, virality, uniqueness) + an exclusivity +1.0 bonus via `exclusivity_bonus()` (Top of Roster / Exclusive, plus hardcoded Tony Danza & The Rocket Man Show). Below-0.40 dropped.
4. **Re-score gig-date news**, any news item the search tagged `hook_type == "upcoming_show"` (a tribute-act live date) is a show announcement, not news, so its score is overwritten with `config.SHOW_BASE_SCORE + exclusivity_bonus(...)`, the same baseline as an Airtable show. This stops exclusivity-boosted gig dates from crowding genuine news (releases, awards, original-artist news) out of every slot. (No-op in news-only mode, where `upcoming_show` items are dropped from the pool entirely.)
5. **Merge, sort, select**, `pool = show_candidates + ranked_news`, sorted by `_score` desc, then `_select_with_diversity(pool, len(week_slots))` (no act more than twice consecutively). When shows + news > slots, the lowest-scoring items (shows *or* news) are dropped.
6. **Generate + post** each selected topic in one unified loop (branch on `_is_show` only for dedup bookkeeping: shows → `mark_show_used()`; news → `mark_topics_used()`). Platforms come from `hook_type` (upcoming_show/tribute_news → all 3; original_artist_news/trivia/historical_fact → IG+FB). `_build_news_post()` returns `None` for `upcoming_show`, so shows and gig dates get no website article.
7. **Fill leftover slots**, in this order: re-bookings, testimonials, act spotlights, agency credibility (Phases 4-7, see "LinkedIn supply" below), then original-artist trivia (`search_trivia()`), then pre-1990 historical facts (`search_historical_facts()`). Driven by `_fill_with_facts()` / `_fill_with_topics()` through the local `_run_phase()` closure, only slots the ranked pool didn't fill, only acts not already scheduled, skipped in `--artist` single-artist mode.

`SHOW_BASE_SCORE` (default 0.75, env-overridable) is the knob that decides how strong a news story must be to outrank a routine show/gig-date: it sits in the normal news-score range so shows and news compete on an **even playing field** (per client direction 2026-07-13). Raise it to protect shows, lower it to favor news. `get_week_slots()` always returns exactly 7 slots (next 7 days at 10am ET). `post_draft_to_buffer()` accepts an optional `scheduled_at: datetime`, when set, uses `schedulingType: "scheduled"` + a Unix timestamp; otherwise queue mode. If the week has no open slots, everything (shows included) is skipped.

**News-only mode (`--news-count N`).** A standalone run mode for filling Buffer with news on demand, independent of the weekly calendar. Sets `news_count` on `main()`, which: (1) **skips show announcements** entirely, both the Airtable calendar show loop (iterates an empty list) *and* tribute-act live-date items surfaced by the news search: candidates with `hook_type == "upcoming_show"` are dropped from the pool before scoring/selection (the same items the website step already skips via `_build_news_post`), so freed slots backfill with real news/trivia/historical and the run still targets N. (Normal weekly runs keep `upcoming_show` news items to promote gigs, the drop is news-only-mode only.) (2) replaces the week-slot list with `[None] * N`, a `None` slot flows through the whole pipeline as "queue this draft" (`post_draft_to_buffer(scheduled_at=None)` → Buffer queue mode, no fixed time), ignoring which slots are already occupied, (3) targets **up to N** posts: artist news first (Phase 3, capped at N via `_select_with_diversity(ranked, N)`), then tops up any shortfall with the fill phases (re-bookings, testimonials, spotlights, agency, trivia, historical facts) to reach N. Fewer than N real news stories → fewer real-news posts, filled to N by the rest. All the slot-formatting log lines guard for `None` ("queue"/"queued"), and `_fill_with_facts()` uses today (ET) as the search-date context when the slot is `None` (needed because `search_historical_facts()` requires a real date). Website news posts, dedup, and cost caps behave exactly as in a normal run. Combine with `--dry-run` to preview.

**Music-news RSS feeds (second news channel).** `lp/feeds.py` reads a curated whitelist of music-outlet RSS feeds (`MUSIC_FEEDS`) as a second candidate-topic source *alongside* Anthropic's hosted `web_search` (used by `search_artist_news()`). RSS is the publisher-sanctioned syndication channel: we read headline + summary + the outlet's own article link, never article bodies. In Phase 1 of `main()`, `load_feed_items()` fetches all feeds **once** (stdlib `xml.etree` + `requests`, no new dependency, never raises, in-process cache, items filtered to the last `FEED_DAYS`=14 days). For each artist, `search_artist_feeds(name, original, feed_items)` returns items whose title/summary mention the tribute act (→ `tribute_news`) or an original artist (→ `original_artist_news`), in the exact dict shape `search_artist_news()` returns, so they flow through the same score → dedup → generate → Buffer path. Feed + web-search results are deduped by url/headline within each artist's batch before `filter_new_topics()`. The **original-artist live-event hard rule** is enforced here too: any `original_artist_news` item matching `_LIVE_EVENT_RE` is dropped. Name matching uses word boundaries and a 4-char minimum to avoid ambiguous short-name collisions; original-artist cells with multiple names are split on `,`/`&`/`/`/`and`. **Whitelist (as of 2026-07-03):** Billboard, Pitchfork (`/feed/feed-news/rss`), Rolling Stone, Stereogum, Brooklyn Vegan, American Songwriter, NME. Pitchfork's feed works from our own server via `requests` (Condé Nast only IP-blocks Anthropic's WebFetch/web_search infra, not us). **Deliberately excluded:** Consequence (robots.txt disallows `/feed/` and blocks ClaudeBot). To add/remove an outlet, edit `MUSIC_FEEDS` in `lp/feeds.py`, verify the feed's robots.txt permits it first. Note tribute-name/original-artist nickname collisions (e.g. "The Fab Four" the act vs. "Fab Four" the Beatles nickname) can classify an item as `tribute_news`; harmless (still relevant, routes to all platforms).

**Always name the tribute act (both channels).** Per client direction (2026-07-16), every social post and every website article must mention the tribute act by name, even when the story is about the original artist (original_artist_news / trivia / historical_fact), tie the story back to the LP act. `generate_posts()` and `generate_article()` both resolve the tribute act as `topic["_act"]` (falling back to `topic["artist"]`, because for original-artist news the `artist` field can be the *original* artist's name, not the tribute act) and set that as the "Tribute Act:" prompt line. On the **website article only**: every article ends with a booking CTA phrased `"If you're interested in booking <act>, please follow this link to set up an appointment with Steve Love."` where **"this link"** (not Steve's name, client preference 2026-07-16) is an inline HTML anchor to `config.STEVE_CALENDAR_LINK` (env `STEVE_CALENDAR_LINK`; `.env` also has a `BEN_CALENDAR_LINK`, currently unused) and "Steve Love" stays plain text. The tribute act's name is mentioned in the body as **plain text, no hyperlink** (client direction 2026-07-16: the "Read more" button already links out, and the reader is already on loveproductions.com, so an inline act-page link is redundant). The "this link" CTA anchor is the **only** link allowed in the article body, no other links/raw URLs; the "Read more" button still carries the source link. (`generate_article()` no longer takes `artist_url`; `_build_news_post` still uses `artist_url` for the button-URL fallback via `_news_button_url`.) The plugin's `wp_kses_post`/paragraph pipeline preserves the `<a>` tags (no plugin change needed). Social posts can't hyperlink a name, so they just name the act in the copy. In the Buffer→website backfill (`_buffer_posts_to_news`), `_match_artist()` (renamed from `_match_artist_url`, now returns the matched Airtable artist dict) sets `topic["_act"]` so backfilled articles also get the tie-in + booking CTA + linked name. If `artist_url` is empty the act name stays plain text; if `STEVE_CALENDAR_LINK` is unset the CTA still appears with "this link" as plain text.

**Artist page URLs (`lp/artist_links.py`).** Every act's loveproductions.com page lives under the site's `/title-item/` directory. The primary source of `artist_url` is the Airtable scan (`_artist_url_from_fields`), but to guarantee the tribute-act anchor above never blanks at runtime, `lp/artist_links.py` holds a prepopulated `ARTIST_PAGE_URLS` map (act name → title-item URL) covering the full current roster (all 24 acts, each verified HTTP 200 on 2026-07-16). `fetch_airtable_artists()` now falls back to `lookup_artist_url(name)` (case-insensitive, whitespace-tolerant) when the Airtable row has no link. Airtable's own value always wins; only add an entry to the static map when a new act's Airtable link is or might be missing.

**Platform routing by hook type.** All routing lives in **one** function, `_platforms_for(hook_type)` in `main.py`, used by both the ranked-pool loop and every fill phase. Three cases:
- **LinkedIn only:** `agency_proof`. An agency-level post is not about any one act, so it has no place on the fan channels.
- **Instagram + Facebook only:** also `tour_poster` (see "Visual floor" below).
- **Instagram + Facebook only:** `original_artist_news`, `trivia`, `historical_fact`, `upcoming_show`.
- **All three:** everything else, i.e. `tribute_news`, `rebooking`, `testimonial`, `act_spotlight`.

`hook_type` is set by Claude during search (`search_artist_news()`), or hardcoded by the source that builds the topic (`search_trivia()`, `search_historical_facts()`, `search_testimonials()`, `build_act_spotlight_topic()`, `build_agency_topic()`, `fetch_rebookings()`). **LinkedIn is a proof/credibility channel, not a show flyer** (client direction 2026-07-24): the client found a long show-announcement LinkedIn post too long and too AI-sounding, so show/date announcements (`upcoming_show`) no longer post to LinkedIn at all. `generate_posts()` still returns a `linkedin` key for every topic; `_platforms_for()` decides which platforms actually receive it, and the unused drafts are discarded.

**LinkedIn supply: four buyer-proof topic types (2026-07-28).** LinkedIn was starved by design, `tribute_news` was the only hook type that reached it and tribute acts rarely make press. Rather than add hooks over the same scarce input, four types were added that source LinkedIn-eligible topics from data we already hold. They fill week slots the ranked news pool leaves open, ahead of trivia and historical facts.

| Hook type | Source | Cadence control |
| --- | --- | --- |
| `rebooking` | `fetch_rebookings()` in `lp/airtable.py` | `config.REBOOKING_MAX_PER_RUN` (default 1) + `sheet_key` fires once per act/venue pairing, ever |
| `testimonial` | `search_testimonials()` in `lp/ai.py` | Source verification drops nearly everything; one per act per run |
| `act_spotlight` | `build_act_spotlight_topic()` in `lp/ai.py` | Quarter-stamped `sheet_key`, one per act per quarter |
| `agency_proof` | `build_agency_topic()` in `lp/ai.py` | Quarter-stamped `sheet_key` + `max_items=1` |

- **Re-bookings.** `fetch_rebookings()` pulls every fully-executed row from the calendar table (`_fetch_calendar_records()`, shared with `fetch_upcoming_shows()`), groups by act + venue, and emits a topic for any pairing with 2+ distinct show dates. Venue names come from `fetch_venue_from_contracts()`, called only for pairings that already qualify, so it is one lookup per re-booking rather than one per contract row. Sorted strongest first (most bookings, then most recent). **Client direction 2026-07-28: use sparingly**, hence the per-run cap. Inspect with `python main.py --test-rebookings` before trusting the grouping.
  - **The venue must resolve to a name, or the pairing is dropped.** "8901 N Kings Hwy has booked them five times" is not a usable post. Names come from `lookup_venue_name()` in `lp/sheets.py` (tour dates sheet) first, then `fetch_venue_from_contracts()`. **Note: the contracts lookup currently 403s**, the Airtable key has no access to the "LPI - Contracts" table, so in practice the tour dates sheet is the only working source. On 2026-07-28 that yielded 10 named pairings out of 33 detected; the other 23 were dropped. Restoring contracts access would recover most of them. `_tour_tab_rows()` caches tab rows per run, since this is dozens of lookups where `lookup_ticket_url()` was previously one.
- **Testimonials.** `search_testimonials()` asks Haiku for a genuine published quote (venue, promoter, buyer, or a real review) and returns `[]` far more often than not, which is the intended behavior. **Anti-fabrication is enforced in code, not in the prompt:** any item missing `quote`/`url`/`attribution` is dropped, and the surviving quote must be found on the cited page by `verify_quote_on_page()` in `lp/scrape.py` (normalizes case, smart quotes, dashes and whitespace, then matches the first 40 characters). It fails closed, so an unreachable page or a paywall drops the testimonial. The verified quote and its attribution are folded into `summary` so `generate_posts()` builds the post on them, and `hooks.md` forbids altering anything inside the quotation marks.
- **Act spotlights.** Evergreen, no search call. `fetch_page_prose()` pulls the `<p>` copy from the act's own loveproductions.com page (paragraph extraction, not whole-page text, so nav and footers stay out; paragraphs under 60 chars are dropped as chrome), which becomes the `summary`. Returns `None` if the page yields under 200 chars. The act page URL comes from `artist_url`, guaranteed non-blank by the static map in `lp/artist_links.py`.
- **Agency credibility.** No search, no scrape. The topic is a static shell; the actual facts live in `content-skill-graph/engine/agency-facts.md`, which is already in the system prompt. **That file is the allowlist:** `hooks.md` and the file itself both state that no agency fact may appear in a post unless it is written there. Extend it only with client-confirmed facts, since everything in it goes out under the agency's own name. `agency_proof` also gets **no website article** (`_build_news_post()` skips it alongside `upcoming_show`), because `generate_article()` requires a per-act tie-in and booking CTA that an agency post has no act for.

**Reserved LinkedIn slots (`LINKEDIN_RESERVED_SLOTS`, default 2).** The four types above only fill slots the ranked pool leaves open, and in a busy week the pool takes all 7. Confirmed on a real dry run 2026-07-28: every selected topic was `upcoming_show` or `original_artist_news`, so LinkedIn got **zero** posts even with the new types implemented. Before selection, `main()` counts how many of the top-N ranked topics are LinkedIn-eligible via `_platforms_for()`, and if that is short of `LINKEDIN_RESERVED_SLOTS` it shrinks the pool's allocation by the shortfall, handing those slots to the buyer-proof phases. Only kicks in when the pool cannot feed LinkedIn itself (a week full of genuine `tribute_news` reserves nothing), and is skipped entirely in news-only mode, where the caller asked for N news posts. Set `LINKEDIN_RESERVED_SLOTS=0` to give every slot back to shows and news.

**Verified quotes are passed explicitly, not via `summary`.** First attempt folded a testimonial's quote into `summary` and the generator paraphrased it away, producing a "testimonial" post containing no quote. Both `generate_posts()` and `generate_article()` now emit a dedicated instruction block when `topic["quote"]` is set, giving the quote and attribution verbatim and forbidding paraphrase, extension, re-attribution, or grammar fixes (trimming from the end only). Keep it that way: the whole point of `verify_quote_on_page()` is defeated if the copy alters the words afterwards.

**Deferred: get verified tour dates from the lp-tour-dates app, not the sheet.** A cancelled show stays in the tour dates sheet until a human deletes it (The Platters' 2026-10-03 Spartanburg date was cancelled and still listed), and it cannot be checked downstream: Ticketmaster answers a server-side request with **401** and Bandsintown with **403**, so ticket-page verification is a dead end. **Verification belongs in the lp-tour-dates app** (`/Users/jonathankohen/lp-tour-dates`), which scrapes the venue listings and is therefore the only thing that can see a date disappear from source. Client direction 2026-07-31: expose a REST endpoint there and have this engine consume it instead of reading the raw sheet. Until then, `python main.py --verify-dates` narrows the manual check to only the rows that actually reach a poster and flags NO-TICKET and FAR-OUT ones. **Check first whether lp-tour-dates prunes vanished dates or only appends**; if it only appends, that is the actual bug and no amount of downstream checking fixes it.

**Deferred: post-show recap posts.** Looking the Airtable calendar backward 7 days would be cheap and would yield "act X played venue Y last week" results posts. **Deliberately not implemented** (client direction 2026-07-28): the client does not want more long show-shaped posts without real attendance or ticket-sales numbers to lead with. Revisit only when that data exists. Do not re-implement before then.

**Dedup keys: `sheet_key` wins.** `topic_key(item)` in `lp/ai.py` is now the single definition of a topic's dedup key: explicit `sheet_key`, then `url`, then `headline`. `filter_new_topics()` and `mark_topics_used()` both use it, and `mark_topics_used()` writes that key into the Sheets **url column** (matching what `mark_show_used()` already did with `lpc_{LPC #}`). This is what makes the quarter-stamped and pairing-stamped keys above work: synthesized topics whose `url` is a shared act page or the homepage would otherwise all collide on one dedup key.

**Slot-filling helpers in `main.py`.** `_draft_topic()` holds the per-topic work (generate, route via `_platforms_for()`, draft to Buffer, build the website article) and is shared by every fill phase. Two drivers sit on top: `_fill_with_facts()` for per-act searches (`search_fn(act, original, slot_date)`, with `require_original` to gate on an original-artist mapping and `max_items` to cap the phase) and `_fill_with_topics()` for ready-made topics that need no search (re-bookings, agency). In `main()`, the local `_run_phase()` closure runs each phase and consumes only the slots it actually used, so phases chain without manual slot arithmetic.

**Per-platform link/image handling (og:image scraping).** `fetch_og_image()` in `lp/scrape.py` scrapes a topic's source URL for its Open Graph preview image (og:image → og:image:url → twitter:image fallback), resolving relative URLs and returning `None` on any failure. It's cheap, one `requests.get()` with a 5s timeout + regex, **no AI call**, in-run cache by URL, no new dependency. Called once per topic in all three posting blocks of `main.py`; `_image_for(platform, og_image)` then routes the result:
- **LinkedIn**, image-only post, **no link card**. Gets the scraped image as a native asset; if none is found, posts text-only. The source URL is deliberately **omitted** from the LinkedIn body (see `generate_posts()` instruction) so LinkedIn can never render a link-preview card. This is what the client wants.
- **Instagram**, uses the scraped image, falling back to the LP logo placeholder (`config._IG_PLACEHOLDER`) when none is found.
- **Facebook**, gets **no** image asset; the source URL stays in the body so Facebook auto-unfurls its own native link-preview card (with the og:image). `generate_posts()` now weaves the source URL into the **Facebook post only** (previously LinkedIn + Facebook).

`post_draft_to_buffer()` attaches `assets.image.url` for both Instagram and LinkedIn (Facebook never gets an asset). **Unverified:** that Buffer's GraphQL accepts `assets` for LinkedIn channels, confirm with one live (non-dry-run) post; if rejected, the LinkedIn asset/metadata shape may need adjustment (e.g. a `metadata.linkedin` field).

**Trivia.** After the artist news pipeline and the buyer-proof phases fill their slots, the next remaining week slots are filled by `search_trivia()`, surprising, lesser-known facts about the **original artist**, any era, not date-bound (distinct from historical facts, which are pre-1990 archival and date-anchored). Sits just below original-artist news in the hierarchy and **above** historical facts. Instagram + Facebook only (`hook_type = "trivia"`), generated only for artists with an original artist mapping that weren't already scheduled this run, and excluded in `--artist` single-artist mode. Deduped via the same Sheets mechanism as news topics. Hook guidance lives in `content-skill-graph/engine/hooks.md` ("Trivia about Artist Represented by Tribute").

**Historical facts (last phase).** After trivia fills what it can, any still-remaining week slots are filled by `search_historical_facts()`, pre-1990 archival facts about original artists from sources like archive.org, old Rolling Stone, Billboard, etc. These are Instagram + Facebook only, lowest priority, generated only for artists with an original artist mapping, and excluded in `--artist` single-artist mode. Deduped via the same Sheets mechanism as news topics. Shares the `_fill_with_facts()` helper in `main.py` with every other per-act fill phase.

**Original artists' shows are never news (hard rule).** We never announce an original artist's own tour, concert, residency, festival appearance, or any live-performance date and then point to our tribute band, it looks bad. Enforced in two places: (1) `search_artist_news()` instructs the model to never return original-artist live-event news and to set an `is_live_event` boolean per item; (2) a code-level guard in `search_artist_news()` drops any `original_artist_news` item with `is_live_event == True`, regardless of how the model classified `hook_type`. Original-artist *non-live* news (album/release, award, induction, biopic, anniversary, milestone, passing, etc.) is still eligible. The rule and an updated example also live in `content-skill-graph/engine/hooks.md` under "News from Artist Represented by Tribute". Tribute acts' own live dates are unaffected, those are exactly what we promote.

**Ticket links and venue names in show announcements.** `lookup_ticket_url()` in `lp/sheets.py` looks up a ticket URL *and* venue name in the tour dates Google Sheet (`TOUR_DATES_SHEET_ID`) produced by the `lp-tour-dates` repo (formerly `love-automations`; local dir `/Users/jonathankohen/lp-tour-dates`). The sheet has per-artist tabs named by display name (e.g., "Arrival from Sweden" for "Arrival from Sweden: The Music of ABBA") with columns: Date (MM/DD/YY), Venue, City, Region, Country, Ticket URL, Source. Matching is by tab name (substring of show_title, case-insensitive) + date. If no venue name is found there, `fetch_venue_from_contracts()` in `lp/airtable.py` falls back to the "LPI - Contracts" table in the calendar Airtable base (`AIRTABLE_CALENDAR_BASE_ID`), filtering by `LPC #`. The `Venue` field from that table is used to replace the raw address in the headline and summary. If neither source has a venue name, the address is used as-is.

**Website news posts (LP News plugin).** Every topic queued to Buffer is also drafted as a standard WordPress **post** on loveproductions.com. After Buffer drafting in each block (shows, artist news Phase 3, and trivia/historical via `_fill_with_facts`), `_build_news_post()` in `main.py` calls `generate_article()` (a dedicated Sonnet call, separate from the social copy) to produce a 2-4 paragraph web article `{title, body, categories}`, then packages it with an image and a button URL. The batch is sent once at the end of the run via `publish_news_posts()` in `lp/wordpress.py` to the **LP News** WordPress plugin (lives in this repo at `wordpress-plugin/lp-news/lp-news.php`; deployed to loveproductions.com). The plugin creates each post as a **draft** for review, assigns built-in `category` terms, sideloads the featured image, and appends a red "Read more" button.
- **Categories**, chosen from a fixed 11-item list (`NEWS_CATEGORIES` in `lp/ai.py`: Celebration, Celebrity, Theatre, Tour, Condolences, Festival, Interview, Sold Out, Tribute, TV Show, Uncategorized). Hybrid: `default_categories(hook_type)` seeds a deterministic default (e.g. `tribute_news→Tribute`, `upcoming_show→Tour`), then Claude adjusts within the allowed list during `generate_article()`. The plugin re-filters server-side to the allowlist (dropping anything off-list) and creates allowlisted terms that don't yet exist.
- **Button URL ("always link somewhere")**, `_news_button_url()` falls back through: source URL → ticket URL → the act's loveproductions.com page (`artist_url` from Airtable) → the homepage (`config.LP_HOMEPAGE`). Label is always "Read more".
- **No show announcements**, the client does not want show/ticket announcements in the website news section. In the weekly pipeline, `_build_news_post()` returns `None` for any `upcoming_show` topic (no article, no Claude call). In the Buffer backfills (`--news-from-buffer` = drafts, `--news-from-queue` = scheduled/queued posts, `--news-from-week` = sent posts, all three share `_buffer_posts_to_news()`), reconstructed topics carry no `hook_type`, so `classify_show_announcements()` in `lp/ai.py` (one batched Haiku call) flags which Buffer posts are live-event/show announcements and they're skipped. A post counts as a show announcement if its primary purpose is promoting attendance at a specific upcoming live event/venue/date (concert, show, residency, festival, **or a personal/live appearance**), general artist news that merely mentions the tribute act or a release date does not. (A regex helper `is_show_announcement()` exists in `lp/buffer.py` for the expired-draft purge, but it false-positives on tribute-act names containing "concert"/"show" and misses non-keyword events like "personal appearance", so the backfill uses the Haiku classifier instead.) Note: `_extract_earliest_date()` in `lp/buffer.py` now strips ordinal suffixes ("July 3rd"→"July 3") before parsing, previously such dates never parsed, so the expired-show purge silently missed them.
- **Body layout ("like a book").** Per client direction (2026-07-16), article bodies render with **no inter-paragraph gaps** (those gaps rendered inconsistently across devices). `lp_news_text_to_blocks()` → `lp_news_build_book_blocks()` in the plugin emit the narrative paragraphs as **one** `wp:paragraph` block, separated by `<br>` (a hard line break, uniform across devices, not separate `<p>` blocks whose theme margins vary) with every paragraph after the first first-line-indented by `LP_NEWS_INDENT` (two `&emsp;`, ≈ one tab, tunable). The final paragraph, the booking CTA, is kept as its **own** separate paragraph block so it still reads as a distinct new line, then the red "Read more" button block follows. This is a pure rendering change in the PHP plugin; `generate_article()` still returns the same blank-line-separated `body` (its last paragraph must be the CTA, which it already is). **Reformatting existing posts:** the LP News settings page ("Settings → LP News → Maintenance → Reformat existing drafts") re-renders existing **draft** posts into this layout, `lp_news_content_to_paragraphs()` recovers the ordered paragraphs + button block from a post's stored content (handles both the legacy one-`<p>`-per-paragraph layout and the new `<br>`-joined layout, stripping leading indent entities), then rebuilds via `lp_news_build_book_blocks()`. Published posts are untouched; WP revisions make it reversible; repeat runs are no-ops (trimmed-content compare). Requires deploying the updated plugin to loveproductions.com first.
- **Theme Page settings (Header/Footer/Sidebar/Layout).** loveproductions.com runs the **`noxe`** theme (Qode "Select" framework), whose per-post "Page settings" (Post date hide, Layout comments/social-share/navigation, Header show/style/skin/transparent/position/sticky, Footer show/skin/newsletter/widgets/copyright/logo, Sidebar position) are stored as per-post meta. `wp_insert_post` doesn't set them, so LP News posts fell back to the theme's global defaults. Rather than hard-code the theme's private meta keys + value encodings (fragile, unknowable without DB access), the plugin **copies the page-settings meta from a template post**: the admin configures one post with the desired settings and enters its ID under **Settings → LP News → Page settings template**. `lp_news_copy_template_meta()` copies all of the template's post meta onto each new post except a blocklist of WP-internal / plugin-managed keys (`lp_news_template_meta_blocklist()`: `_lp_news_key`, `_thumbnail_id`, edit locks, slugs, attachment meta, etc.). Theme-agnostic and upgrade-proof. **Fixing existing posts:** the settings page offers two apply actions, a **targeted** one (`lp_news_apply_template_to_ids()`, enter specific comma/space-separated post IDs, applies to those posts regardless of whether they're LP News posts) and an **all** one ("Apply to all existing posts", `lp_news_apply_template_to_existing()`), which stamps the template's page settings onto every existing LP News post (published included, since the goal is to fix live posts). Stored in the `lp_news_template_post_id` option; no-op when unset.
- **Image**, **only** the source article's own og:image (`fetch_og_image`). There is deliberately **no fallback** to the artist's profile photo or the LP logo placeholder: if the article has no image, `image_url` is sent as `""` and the plugin drafts the post with no featured image for manual handling. (The plugin already treats an empty `image_url` as "no featured image".)
- **Dedup**, the plugin stores a `_lp_news_key` meta per post (source URL → show sheet key → headline) and skips re-sends, so re-runs never duplicate a story. This is independent of the Sheets/Buffer dedup.
- **Disabled when unconfigured**, if `LP_NEWS_URL`/`LP_NEWS_SECRET` are unset, the website step is skipped (Buffer drafting is unaffected) and the planned posts are logged. `--dry-run` sends `dry_run: true` to the plugin (plans, no writes) when configured.
- **Artist page URLs**, `fetch_airtable_artists()` now requests **all** artist fields (not a fixed subset) and `_artist_url_from_fields()` scans values for a loveproductions.com URL → `artist_url` on each artist dict. This is the source URL fallback for source-less items (trivia/historical/shows without a ticket link). If the artists table has no such field, `artist_url` is "" and the button falls back to the homepage.
- **Cost**, `generate_article()` is a second Sonnet call per topic (on top of `generate_posts()`), gated by the same `COST_CAP_USD` / `CLAUDE_CALL_LIMIT`. ~7 articles/run adds roughly $0.10-0.15.

**Self-learning via performance context.** `fetch_top_performers()` in `lp/buffer.py` delegates to `fetch_meta_top_performers()` in `lp/meta.py`, which queries the Meta Graph API for recent Facebook Page posts ranked by shares + reactions. `format_performance_context()` in `lp/ai.py` formats the top 3 as style examples injected into every `generate_posts()` call. Buffer's own `statistics` field is paywalled, Meta Graph API is the correct data source. Token setup: run `python get_page_token.py` after generating a fresh User Access Token in Graph API Explorer with these permissions: `pages_read_engagement`, `pages_show_list`, `instagram_basic`, `pages_read_user_content`, `pages_manage_posts`. The resulting Page Access Token does not expire. Notes: `me/posts` fails for New Pages Experience pages, use `{page_id}/posts` instead. `reactions.summary(true)` and `comments.summary(true)` require `pages_read_user_content` + `pages_manage_posts` in addition to `pages_read_engagement`.

**Instagram analytics: deliberately not connected (decided 2026-07-31).** Facebook is the sole analytics source. `_fetch_instagram_posts()` in `lp/meta.py` stays in the code and fails gracefully (returns `[]` when `instagram_business_account` is empty on the Page), so nothing is broken; it is simply never populated. Reasoning: the Page → Instagram link is a **UI-only** operation in Meta Business settings, there is no Graph API endpoint that creates it, and it needs full control of the Page plus admin on the IG account. The payoff does not justify the access wrangling: the whole feature is three post texts truncated to 200 chars, injected into `generate_posts()` as soft style examples, and Buffer pushes near-identical copy to both channels anyway, so IG captions would add almost no signal over the Facebook posts already filling those slots. **Do not re-attempt the Business Portfolio route.** If Instagram engagement ever diverges meaningfully from Facebook's and the data is genuinely wanted, the escape hatch is **Instagram API with Instagram Login** (direct OAuth at instagram.com, no Facebook Page link required, needs a Meta app with the Instagram product added and a one-time authorization by the IG account holder), roughly 30 lines in `lp/meta.py`. `instagram_basic` can be dropped from the token permission list above; it is inert without the link.

**Generated tour posters (2026-07-31).** `python make_tour_posters.py` renders one poster per act: the act's own key art above its upcoming dates, in LP's brand frame. Output goes to `assets/tour-posters/generated/` (gitignored with the rest of `assets/`). Flags: `--act`, `--min-dates` (default 4), `--max-rows` (default 12, per column), `--size poster|square|linkedin`, `--allow-residency`, `--out-dir`.

- **Why generated and not found.** The roster's acts do not publish multi-date tour posters. A sweep of 12 acts' Instagram and Facebook feeds (written up in `assets/act-key-art/TOUR-POSTERS-FINDINGS.md`) found only **single-show flyers**: one date each, usually venue or promoter branded, carrying the venue's own ticketing URL, and stale within the week. Bohemian Queen, Back 2 Mac and The Wankers do post dated graphics; Rocket Man, Monkee Men, Love TKO and Vitaly post none. **Do not re-run the "find real tour posters" search**, the answer is in that file. Note an earlier pass checked only act *websites* and ticketing aggregators and wrongly concluded nothing existed; the graphics are on the social feeds.
- **Layout** (`render_tour_poster()` in `lp/cards.py`). 1080x1350 (Instagram's tallest uncropped ratio) by default. Key art fills the top 52%, **contained not cover-cropped**, over a blurred dimmed copy of itself: nearly all key art is square while the panel is landscape, and a cover crop beheaded the act logo, which is the one thing identifying the act. The foot of the art fades into the dark panel that carries "TOUR DATES", the act name, and the rows.
- **Rows.** Two columns when the list outruns one, so a 17-date act does not show 8 dates beside half a panel of dead space. Rows keep a legible floor (`min_row_px`), and anything past the cap becomes a "+ N MORE DATES" line rather than being silently dropped. **Years are printed only when the run crosses a year boundary**: The Platters go Feb '27 → Mar '27 → Feb '28, which reads as a sorting bug without them.
- **`display_name()`** turns Airtable's sort-friendly "Platters, The" into "The Platters" for the poster.
- **Residencies are skipped.** Judged on the dates that will actually be drawn, not the whole list: Reza plays 92 dates in two venues, so a whole-list check passes while every visible row still says "REZA LIVE THEATRE". `--allow-residency` overrides.
- **As of 2026-07-31**, 14 of 24 acts render. The other 10 are skipped for too few upcoming dates (Always Celine 1, Monkee Men 1, Eagle Wings 1, Priscilla Presley 2, Vitaly 3), no dates at all (Elvis: The Concert of Kings, Legends of Pop, Love TKO, Michael Griffin), or residency (Reza).
- **Reads the sheet in one pass.** `lp.sheets.upcoming_tour_dates()` reopens the spreadsheet on **every** call, which trips gspread's read quota partway through a 24-act loop and returns `[]` for the acts that come later, silently, indistinguishable from an act having no dates. `make_tour_posters.py` therefore does its own `load_all_tabs()` fetch. **Worth fixing in `lp/sheets.py` itself**, since any caller looping over the roster hits this.
- **Tab matching.** The sheet's tab names drift from the roster's ("The Dolly Show" against "Dolly Show, The", "Concert of Kings" against "Elvis: The Concert of Kings"). `match_tab()` tries exact slug, then containment, then a word-subset comparison with the leading article dropped. The subset step requires every sheet word to appear in the act name so "Legends of Pop" cannot claim the "Legends of Classic Rock" tab.
- **Not yet wired into `main.py`.** These render to disk only. Posting them needs `upload_media()` for hosting and a decision on cadence. `_draft_tour_carousel()` in `main.py` already posts the *other* format (`render_tour_card()`, dates over a darkened act photo, one slide per act as a carousel), which is a separate thing modelled on the Reliant Talent post the client sent.

## IN PROGRESS: visual post types (2026-07-31)

**Read this before starting work.** This section is the live state of an unfinished sprint. Update it as things land; do not wait for the end of a session.

**Why.** The client rejected a five-paragraph Rocket Man Show LinkedIn draft outright: "I'm so sorry but I just think this is so AI." Their direction, verbatim in substance: several paragraphs about a show does not sell to someone already in the business; it needs "a quick short sales" pitch; "maybe some quick video, maybe some hard numbers with a graphic"; "if we picked up four LinkedIn followers in a month, something's not right"; "I wanna be on the edge with content creation, not just going along with the flow." The rejected draft's failure mode is instructive: five paragraphs, an em dash, the banned "not just X, they're Y" construction twice, and the only concrete proof (Rus Anderson owns Elton John's actual 1970s costumes) buried under generic agency-speak.

All 134 Buffer drafts were deleted on 2026-07-31 to start clean. There is no draft backlog to preserve, and whatever ships next is what the client sees.

**Landed.**
- `extract_page_quotes()` in `lp/scrape.py` harvests attributed pull-quotes (buyer praise, press) from act pages. Twelve acts carry them. The agency published them itself, so they are pre-cleared and skip the `verify_quote_on_page()` round trip. Strictly better than `search_testimonials()`, which costs a web-search call and correctly returns nothing most of the time; try page quotes first, fall back to the search.
- `lp/ai.py` turns those into `testimonial` topics, buyer quotes ahead of press, dedup key `pagequote_<slug>` so each individual quote fires once ever and an act with several quotes can recur in later runs with a different one.

**Cards: landed and wired in (2026-07-31).**
- `lp/cards.py`, Pillow-rendered branded cards: `render_quote_card()` and `render_stat_card()`. Deterministic (the filename is a hash of act + copy + size + background URL, so re-runs reuse the file and do not churn Buffer assets), act photo darkened behind the text, brand red and cyan sampled from the site's stylesheets, two sizes (1200x627 for LinkedIn, 1080x1080 square for IG/FB). `Pillow>=10.0` in `requirements.txt`. PNGs land in `config.CARD_DIR` (`cards/`, gitignored, env-overridable).
- **Hosting solved.** The LP News plugin gained a second route, `/wp-json/lp-news/v1/upload-media` (`lp_news_rest_upload_media()`), which takes `{key, filename, content_base64}` and returns the attachment URL. Same `X-LP-News-Secret` auth as publish-news. It accepts png, jpg and **mp4**, validating image bytes with `getimagesizefromstring` rather than trusting the caller's extension, and dedups on `_lp_news_media_key` so a re-uploaded card returns the existing attachment. **The mp4 path and the Buffer `video` asset are verified only in dry run so far**: confirm the deployed plugin on loveproductions.com is current and post one live clip before trusting `--visual-count` unattended. `upload_media()` in `lp/wordpress.py` is the client; its default key is a SHA1 of the file contents, and it caches per run. **The updated plugin must be deployed to loveproductions.com before cards can post.**
- **Which topics get a card** (`_cards_for_topic()` in `main.py`): `testimonial` → quote card; `rebooking`, `agency_proof`, `act_spotlight` → stat card. News, trivia and historical facts keep the source article's og:image, which is the right picture for a story about someone else. Card backgrounds come from `fetch_act_photo()`, **not** `fetch_og_image()`: act pages carry no og:image tags at all, so the hero shot has to be found in the markup.
- **Image routing** (`_image_for()`): a card outranks the scraped og:image everywhere. Facebook still normally gets no asset (so its native link preview renders), **except** when the topic has no source URL to unfurl, which is exactly the card topics, where the card is the only thing to show. The website article also uses the card as its featured image when there is one.
- `fetch_act_photo(url, act)` in `lp/scrape.py` now **scores** candidates instead of taking the widest. Widest alone picked promo slicks and video covers whose own headline type collided with the card's. It now prefers files whose name matches the act and penalises `_ARTWORK_RE` names (ad, poster, flyer, cover, banner, logo, promo, thumbnail), with width as the tiebreaker. Verified correct for Arrival From Sweden, The Rocket Man Show and The Dolly Show.

**Stat sources (`lp/stats.py`), all three wired.** Every figure traces to something already verified; nothing is invented, estimated or rounded.
- `agency_stats(year)` parses the bullets under `## Verified` in `agency-facts.md`, so that file stays the single source of truth and a fact the client revokes stops appearing on cards with no code change. Years in business is derived from the founding year, so it never goes stale. Currently yields 41 years / 180+ acts / 70+ countries.
- `rebooking_stat(topic)` needs **3+** bookings: two is a coincidence and reads as weak proof. `fetch_rebookings()` now also carries `_venue` and `_span` as discrete fields so the number does not have to be re-parsed out of the summary. **Caveat: as of 2026-07-31 only one pairing in the data has 3+ bookings** (the rest are 2x), so this card is rare in practice, and rarer still after that pairing's once-ever dedup key fires. This is downstream of the contracts-table 403 documented above; restoring that access would widen it.
- `act_credential_stats(artist)` extracts up to one hard number from the act's own page via Haiku (no `web_search` tool: the page is the only permitted source). **Two code-level guards, not prompt promises:** the value's digits must literally appear in the page prose, and a bare calendar year is rejected ("1973 / year of iconic costumes worn" is a date, not proof). Attached to spotlight topics by `_with_credential_stats()` in `main.py`. Real results: Arrival From Sweden "100 sold-out tours in the US", The Dolly Show "25 years performing professionally", The Platters correctly returns nothing.

**Video: landed 2026-08-03.** `lp/vimeo.py` cuts the clips (see "Vimeo clips" below) and they now post: `post_draft_to_buffer()` takes `video=` (plus an optional `thumbnail=`) and sends an `AssetInput` with a `video` member, and the LP News `/upload-media` route accepts `mp4` alongside `png`/`jpg`. Clips reach Buffer through the visual phases only, never through the news path.

**Client answers on voice and format (2026-07-31).** Steve Love is the main client: agency boss and head agent.
- **LinkedIn is written to buyers only.** Not a mixed buyer/fan audience.
- **The stated goal is follower growth**, not inbound enquiries. Jonathan's own view is that this is the wrong metric, but it is what the client asked for, so optimise for it.
- **Length: 200 to 300.** Jonathan said "words", but every other signal says characters: the rejected post was about 180 words and the client called it far too long, and the model post he pointed to (below) is 15 words. **Treat 200-300 characters as the target and confirm before writing anything long.**
- **The LinkedIn CTA is Steve's calendar link** (`STEVE_CALENDAR_LINK`), not `info@loveproductions.com`. This matches the website articles.
- **Images are an add-on, not a requirement.** Card posts are one post type among several; text-only posts stay legitimate. Do not gate every post on having a graphic.
- **Do not use Steve's own writing as a voice sample.** Explicitly rejected. The brand voice has to come from the published site copy and the examples below, not from client emails.
- **What the client has liked:** the news posts tying tribute acts to their original artists (the Monkees conference and the new Tom Petty releases were both named). That is evidence for `tribute_news` and `original_artist_news`, the hook types already in the pipeline.

**The model post the client sent (Reliant Talent Agency).** Steve pointed to [this Instagram post](https://www.instagram.com/p/DbMQ4wrknkD/) as the thing to emulate. Verified directly on 2026-07-31:
- **Caption in full:** "Our clients are booked and busy! Swipe to see some of the tours that are on sale now." Two sentences, 17 words, then three hashtags. No paragraphs, no link, no pitch, no credentials.
- **Format: a 7-slide carousel.** Each slide is one act's **official tour admat**, the full date list rendered as a poster, with **SOLD OUT flagged in red** next to individual dates.
- **The proof is the artwork, not the copy.** The dates and the sold-out flags carry the entire message; the caption just tells you to swipe. This is the sharpest available answer to "hard numbers with a graphic".
- Reliant has ~2,900 followers and that post got 68 likes, so this is a format worth copying, not evidence of a channel that is winning. Manage expectations about follower growth accordingly.
- **Implication for us:** LP has per-act tour dates already, in the tour dates sheet (`TOUR_DATES_SHEET_ID`, tabs per act with Date / Venue / City / Region / Country / Ticket URL). A "tours on sale now" carousel is buildable from data we hold. **Unverified: whether Buffer's GraphQL `assets` field accepts multiple images for a carousel.** `post_draft_to_buffer()` currently sends a single `assets.image.url`. Confirm before designing the post type around it.

**Buffer carousels are supported (verified 2026-07-31).** Schema introspection confirms `CreatePostInput.assets` is typed `[AssetInput!]!`, a list. The long-standing single-dict form worked only because GraphQL coerces a lone value into a one-element list. `post_draft_to_buffer()` now takes `images: list[str]` alongside `image`, and attaches `[{image: {url}}, ...]`. Facebook also receives assets now **when a topic has no source URL to unfurl**; it still gets none otherwise, so its native link preview keeps working. `AssetInput` also has a `video` member (`VideoAssetInput`: `url`, `thumbnailUrl`, `metadata{title, thumbnailOffset}`), so video posting needs nothing beyond a public mp4 URL.

**Tour carousel (`--tour-carousel`).** `_post_tour_carousel()` in `main.py` builds the Reliant-style post: one tour-date slide per act, Instagram gets the full carousel and Facebook the album. **LinkedIn gets nothing** (2026-08-03; it used to take the lead slide, but a poster of dates on sale is a show announcement whatever the artwork). Slides come from `render_tour_poster()` in `lp/cards.py` over `upcoming_tour_dates()` in `lp/sheets.py`. **The caption is hardcoded, not generated:** "Our acts are booked and busy. Swipe to see the tours on sale now." A model asked to write around a date list pads it straight back into the paragraphs the client rejected. Instagram gets the caption alone (captions do not linkify, so a 140-character booking URL is dead weight); Facebook gets a `Booking:` line with `STEVE_CALENDAR_LINK`. Verified on real data: 8 slides, 65-character Instagram caption.
- **Two exclusions, both learned from real output:** an act with fewer than 3 upcoming dates does not read as "on tour", and an act whose dates are all at one venue is a **residency**, which renders as a dozen identical rows and looks like a bug (Reza hit this exactly).
- `upcoming_tour_dates()` also **normalises regions** ("New York" and "ny" both become "NY") and **dedups**, because the hand-maintained sheet had the same Schenectady show entered twice in two formats, which read as a double booking.

**Vimeo clips (`lp/vimeo.py`, `--test-clips`).** The agency has ~1,600 videos on Vimeo and 23 of 24 roster acts are represented, mostly promo reels. It is the agency's own footage, so the rights are already clean, which is the whole reason it beats generating video. `clip_for_act()` finds an act's best promo, cuts a 15-second 1080x1080 mp4 with ffmpeg, and returns the path; `upload_media()` then hosts it. **Requires ffmpeg/ffprobe on PATH** and a token with the `video_files` scope (without it the API omits download links).
- **Never download the whole source.** The rendition URL goes straight to ffmpeg, which seeks with HTTP range requests, so a clip costs a few MB instead of hundreds. The first implementation pulled a 900MB file to use 15 seconds of it.
- **The `source` rendition is excluded deliberately.** Vimeo reports it at the same height as the transcoded copy but it is the untouched master (940MB vs 234MB for one act), and it won a naive tie-break on height.
- **Clips are padded, not centre-cropped.** These promos carry burned-in titles and lower thirds, and a square centre crop slices them in half ("...OF ABBA / ...IVAL / ...WEDEN"). The frame is fitted whole over a blurred, darkened copy of itself.
- **Dated venue ads are filtered out** (`_DATED_AD_RE`). Some videos are cuts made to advertise one booking, with a burned-in date and venue; reposting those advertises a show that already happened. Confirmed live on a Bohemian Queen cut reading "FEBRUARY 23 ... TICKETS ON SALE FRIDAY".
- **Start position:** `max(10s, 15% in)`, capped clear of the end card. A 3-second skip still landed on the LP logo animation these reels open with.
- **`_MIN_DURATION` is 60s**, well above the clip length, because a 30-second promo is mostly titles and an end card with nothing usable in between.
- **Still human-gated.** Source selection is heuristic. Run `python main.py --test-clips [--artist NAME]` and watch the output before wiring clips into an unattended posting path.

**Em dashes are stripped in code, not asked for in the prompt (2026-07-31).** The rule appears in `humanizer.md`, in `linkedin.md`, and in both `generate_posts()` and `generate_article()`, and the model still produced **seven em dashes across seven posts** in a full dry run. Asking is not enough. `strip_dashes()` in `lp/ai.py` now removes every em and en dash from post copy, article titles and article bodies: a dash before a capitalised word becomes ". " (it was doing a full stop's job, e.g. "Winter Garden, FL—The Rocket Man Show opens..."), everything else becomes ", ". It is the last thing to touch the copy, so nothing downstream can reintroduce one, and it leaves inline HTML alone. **The old `replace(" — ", "—")` in `main.py` was actively harmful**: it normalised the spacing around an em dash while preserving the dash itself, so it made violations harder to spot rather than fixing them. Both call sites now use `strip_dashes()`.

**First live carousel run (2026-07-31): the whole path works.** 8 posters rendered, all 8 hosted through `/upload-media`, 3 Buffer drafts created (Instagram 8 slides, Facebook 8, LinkedIn 1). All 8 hosted URLs verified live, HTTP 200 with an image content-type.

**loveproductions.com converts uploaded PNGs to JPEG.** Something on the WordPress side (an image optimiser) rewrote 7 of the 8 uploads to `.jpg`; `wp_get_attachment_url()` returns the post-conversion URL, so `upload_media()` hands Buffer the right link and nothing extra is needed. **But one of the eight came back still `.png`**, and it is not a transparency case (both files are plain RGB), so the conversion looks asynchronous and that upload had not been processed yet. **Watch for this:** if the optimiser converts that file after we have read its URL, the `.png` link already stored in a Buffer draft could go stale. It served 200 as PNG when checked. If a draft ever shows a broken image, this is the first thing to check.

**No show content on LinkedIn, at all (2026-08-03).** Routing by `hook_type` was not enough. `_platforms_for()` already kept `upcoming_show` off LinkedIn, but a gig date can arrive from the news search classified as `tribute_news` (the model gets it wrong often enough to matter) and then sails through as ordinary news. So the generated copy is checked as well, in `_linkedin_show_block(topic, text)` in `main.py`, which drops the LinkedIn draft (the other two channels still get it) when the topic is a show, carries a `ticket_url`, or the copy reads as a pitch to attend a date still ahead of us. That last test is `is_future_show_announcement()` in `lp/buffer.py`, deliberately stricter than the existing `is_show_announcement()`: buyer-proof copy cites **past** dates ("this venue has booked them three times") and legitimately says "venue" and "show", and LinkedIn is the starved channel, so a false positive there costs a post. `tour_poster` is also `_NO_LINKEDIN_HOOKS`, and the tour carousel no longer posts to LinkedIn either. Verified: the guard blocks a dated `tribute_news` gig post and passes re-booking, spotlight and testimonial copy unchanged.

**Visual floor (`VISUAL_MIN_PER_WEEK`, default 2) and visual-only mode (`--visual-count N`).** Client direction 2026-08-03: "we need more images and video, can we set a minimum for those types of posts a week, maybe 2?" A **floor, not a cap**. Card-eligible material is genuinely scarce (the 2026-07-31 dry run produced zero cards across 16 drafts), so after every other phase `main()` counts the topics flagged `_visual` (a topic that got a rendered card, poster or clip; a scraped og:image does **not** count, it is someone else's article thumbnail) and makes up the shortfall through `_draft_visual_posts()`.

- **Two sources, neither of which needs a story to exist.** `_tour_poster_topic()` renders an act's upcoming dates via `render_tour_poster()` over the tour dates sheet, with the carousel's exclusions (3+ dates, no residency). `_act_clip_topic()` cuts a clip from the agency's Vimeo library. They are **interleaved** by `zip_longest`, so a floor of 2 comes back as one poster and one clip rather than two posters.
- **Routing:** `tour_poster` is Instagram + Facebook (it announces dates). `act_video` goes to **all three**, including LinkedIn: it is the act performing with no date attached, so it is the one visual type that is not a show announcement.
- **Captions are hardcoded, not generated**, for the same reason the carousel's is. "On tour now: <act>. Dates on the graphic." and "<act>, live." Fronted rather than "<act> is out on the road", because half the roster is plural ("The Platters is...") and half is not. Instagram gets the caption alone; Facebook and LinkedIn get a `Booking:` line.
- **The floor runs even when the week has no slot left**; the extras queue behind the schedule rather than being dropped. Skipped in single-artist mode and in news-only mode (where the caller asked for exactly N news posts).
- **Dedup:** posters are month-stamped (`tourposter_<slug>_<YYYY-MM>`) so an act's poster refreshes as its dates move; clips are quarter-stamped (`clip_<slug>_<YYYYQn>`).
- **`--visual-count N`** is the same machinery with everything else switched off: N graphic/video posts, queued with no fixed schedule, no news search, no scoring, no website articles, and **no Claude calls at all**. Verified end to end on a dry run.

**Show announcements are capped (`MAX_SHOWS_PER_WEEK`, default 3).** Client direction 2026-07-31: "we shouldn't flood the drafts with show announcements, maybe max three a week." Enforced in `_select_with_diversity()` via `_is_show_topic()`, which counts **both** Airtable calendar shows and gig-date news, since they score the same and read the same to a follower. The cap holds even in the fallback branch that relaxes the consecutive-act rule: filling a slot with a fourth show is precisely what it exists to prevent, and freed slots backfill through the normal phases. Confirmed on a real dry run: 3 selected, **8 held back**, and the freed slots went to genuine news (a Priscilla Presley appearance, Micky Dolenz) that had previously lost every slot to shows. Why the cap rather than a scoring tweak: shows and gig dates score `SHOW_BASE_SCORE + exclusivity` (1.75 for an exclusive act) where real news lands around 0.3 to 0.8, so news can essentially never outrank a show on score alone.

**Act credentials: derive years-active in code, do not ask for it.** The extraction guard rejected "7 decades performing" for The Platters, which was actually a *correct* derivation from the 1953 on their page. The guard was right to refuse the model's arithmetic; the fix is to stop asking for arithmetic. `_ACT_SINCE_RE` in `lp/stats.py` finds a founding/performing-since year literally on the page and `act_credential_stats()` computes the difference itself, before any model call. This also fixed the "cards almost never fire" problem, since a founding year is the most common real number on these pages. **Keep the extraction guard**: it only applies to the one source where a model reads a page and reports a number. Re-booking counts (Airtable) and agency facts (the allowlist file) are structured data and are correctly not checked.

**`display_act()` in `lp/cards.py` un-inverts filing-order names for display.** Airtable files two acts alphabetically ("Platters, The", "Dolly Show, The"), which is right for a list and looks like a database error on a graphic. Display only: the stored name is untouched, because dedup keys, the `artists.md` mapping and the tour sheet tab names all match on it.

**First full dry run of the new pipeline (2026-07-31), what it showed.** 16 Buffer drafts across 7 slots. Findings worth keeping:
- **Zero cards rendered in the entire run**, and both misses were correct behaviour, not bugs. The only re-booking that surfaced was a 2x pairing (below `rebooking_stat()`'s 3+ floor), and the act spotlight's credential stat ("7 decades performing" for The Platters) was **dropped by the verification guard** because the digit 7 does not appear in the page prose; the page says 1953 and the model inferred the rest. The guard did its job. The lesson is that **card-eligible material is genuinely scarce in a typical week**, so the tour carousel, which depends only on the tour dates sheet, is the reliable visual vehicle, not the stat cards.
- **5 of the 7 slots went to `upcoming_show` topics**, all scoring 1.75 (`SHOW_BASE_SCORE` 0.75 plus the 1.0 exclusivity bonus). None of the top 7 ranked topics were LinkedIn-eligible, so `LINKEDIN_RESERVED_SLOTS` kicked in and LinkedIn got exactly 2 posts (a re-booking and an act spotlight). If LinkedIn supply matters more than show coverage, the lever is `SHOW_BASE_SCORE` down or `LINKEDIN_RESERVED_SLOTS` up.
- **Watch the Platters spotlight copy.** It asserted the act is "not a tribute, the official organization". That may well be true of the licensed group, but it is a factual claim about an act, generated from scraped page prose, and it should be checked by a human before it goes out under the agency's name.

**Overlap warning (2026-07-31).** A second session was working in this repo at the same time and added `render_tour_poster()` in `lp/cards.py`, `make_tour_posters.py`, and `assets/act-key-art/` (official act key art, better than the scraped act photos `render_tour_card()` uses). **`render_tour_card()` and `render_tour_poster()` overlap and should be reconciled into one.** Prefer the key-art source; check `key_art_for()` before scraping a page photo.

**Also queued: full skill-graph rewrite.** Before mass drafting resumes, the `content-skill-graph/` markdown is to be **rewritten, not patched**, grounded in real published loveproductions.com copy. Interview the client contact first with comprehensive questions about how content should be made, and ask which specific kinds of real examples they can supply. `brand-voice.md` has an HTML-comment placeholder for pasting more client-approved examples.

## Environment variables (`.env`)

```
ANTHROPIC_API_KEY
BUFFER_API_KEY
AIRTABLE_API_KEY
AIRTABLE_BASE_ID        # default: appMMwX47V1g2Sv5u
AIRTABLE_ARTIST_TABLE   # default: tbloEhiPP4kyTTVDb
FOUND_NEWS_STORIES_SHEETS_ID
TOUR_DATES_SHEET_ID     # optional: tour dates sheet from the lp-tour-dates repo (formerly love-automations) (for ticket links)
COST_CAP_USD            # default: 5.00
SHOW_BASE_SCORE         # default: 0.75, baseline score for shows/gig-dates in the ranked pool (see "Scoring and scheduling"); raise to protect shows, lower to favor news
REBOOKING_MAX_PER_RUN   # default: 1, max "back by popular demand" posts per run; the client wants these rare
LINKEDIN_RESERVED_SLOTS # default: 2, week slots held back for LinkedIn-eligible buyer-proof content when shows/news would take them all; 0 disables
MAX_SHOWS_PER_WEEK      # default: 3, hard cap on show announcements per week (Airtable shows + gig-date news together)
VISUAL_MIN_PER_WEEK     # default: 2, floor (not a cap) on graphic/video-led posts per week; 0 disables the top-up phase
GOOGLE_APPLICATION_CREDENTIALS_JSON  # service account JSON string (CI/Actions)
LINKEDIN_ANALYTICS_CSV  # optional: path to manually exported LinkedIn analytics CSV
LP_NEWS_URL             # optional: LP News plugin endpoint, e.g. https://www.loveproductions.com/wp-json/lp-news/v1/publish-news
LP_NEWS_SECRET          # optional: secret from the WP "Settings → LP News" page (X-LP-News-Secret header). Unset → website posting skipped
STEVE_CALENDAR_LINK     # optional: Steve Love's booking-appointment scheduling URL; the "Steve Love" anchor in every website news article's closing booking CTA points here. Unset → CTA still appears, name is plain text
VIMEO_CLIENT_ID         # LP's own act footage, source for the short clips cut by lp/vimeo.py
VIMEO_ACCESS_TOKEN      # needs the video_files scope or the API omits download links
VIMEO_CLIENT_SECRET
```

## LinkedIn analytics (self-learning)

LinkedIn API access was applied for but not approved. Instead, export analytics manually:

1. Go to the LinkedIn Company Page admin → **Analytics → Updates**
2. Click **Export** (top right) to download a CSV
3. Set `LINKEDIN_ANALYTICS_CSV=/path/to/export.csv` in `.env`
4. Run `python main.py --test-analytics` to confirm LinkedIn posts appear in the output

`lp/linkedin.py` parses the CSV and feeds the top performers into `fetch_top_performers()` alongside Meta posts. Re-export monthly (or whenever) to refresh the data. The CSV column names LinkedIn uses are: `Post title`, `Impressions`, `Likes`, `Comments`, `Shares`.

## Adding or editing artists

Edit `content-skill-graph/engine/artists.md`. The table maps `Tribute Act | Original Artist`. Leave Original Artist blank if not applicable. Changes take effect on the next run, no code change needed.

## Extending the skill graph

Drop a new `.md` file anywhere under `content-skill-graph/`. It gets picked up automatically by `load_skill_graph()` (alphabetical sort). Use the existing files as style reference, keep them instructional, not descriptive.

## Formatting

When editing the markdown docs, or writing anything client-facing, ***please*** don't use any em dashes at all. Always consider a full stop, new sentence, semi-colon, whatever works. Anything but em dashes. And please try to make what you write as succinct as possible, while still being effective. Do your best to keep things short.